package com.grindscale.android.ui.theme

import androidx.compose.ui.graphics.Color

object CoffeeColors {
    val background = Color(0xFFF7F0E6)
    val card = Color(0xFFFAFAF2)
    val cardStroke = Color(0xFFE0CCCC)
    val amber = Color(0xFFB87A47)
    val deepBrown = Color(0xFF59381F)
    val accent = Color(0xFFE68538)
    val muted = Color(0xFF736157)
    val labelBlack = Color(0xFF000000)
    /** Chart line (matches iOS `CoffeeTheme.amber` on histogram stroke). */
    val amberChart = amber
    val chartGrid = Color(0x14000000)
}
