# LINE LIFF ID from LINE Developers Console (empty = dev mode without LIFF)
VITE_LIFF_ID=

# API base URL (empty = same origin; dev uses Vite proxy to :8000)
VITE_API_BASE=

# Vite dev proxy target
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
