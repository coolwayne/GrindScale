{
  "name": "grindscale-liff",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pages:deploy": "wrangler pages deploy dist --project-name=grindscale-liff1 --branch=main",
    "build:pages": "npm run build && npm run pages:deploy"
  },
  "dependencies": {
    "@line/liff": "^2.27.3"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "wrangler": "^3.114.17"
  }
}
